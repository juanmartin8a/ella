///
/// JHybridEllaTerminalViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridEllaTerminalViewStateUpdater.hpp"
#include "views/HybridEllaTerminalViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::ella::terminal::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridEllaTerminalViewState>;

void JHybridEllaTerminalViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridEllaTerminalViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  std::shared_ptr<JHybridEllaTerminalViewSpec> hybridView = javaView->getJHybridEllaTerminalViewSpec();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridEllaTerminalViewState& data = concreteState->getData();
  const std::shared_ptr<HybridEllaTerminalViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    // Props aren't set yet!
    throw std::runtime_error("HybridEllaTerminalViewState's data doesn't contain any props!");
  }

  // Update all props if they are dirty
  if (props->headerInset.isDirty) {
    hybridView->setHeaderInset(props->headerInset.value);
    props->headerInset.isDirty = false;
  }
  if (props->onConnectionStateChange.isDirty) {
    hybridView->setOnConnectionStateChange(props->onConnectionStateChange.value);
    props->onConnectionStateChange.isDirty = false;
  }
  if (props->onHostKeyRequest.isDirty) {
    hybridView->setOnHostKeyRequest(props->onHostKeyRequest.value);
    props->onHostKeyRequest.isDirty = false;
  }

  // Update hybridRef if it changed
  if (props->hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props->hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
    props->hybridRef.isDirty = false;
  }
}

} // namespace margelo::nitro::ella::terminal::views
