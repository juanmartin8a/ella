///
/// HybridEllaTerminalViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridEllaTerminalViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::ella::terminal::views {

  extern const char HybridEllaTerminalViewComponentName[] = "EllaTerminalView";

  HybridEllaTerminalViewProps::HybridEllaTerminalViewProps(const react::PropsParserContext& context,
                                                           const HybridEllaTerminalViewProps& sourceProps,
                                                           const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    headerInset([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("headerInset", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.headerInset;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.headerInset);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("EllaTerminalView.headerInset: ") + exc.what());
      }
    }()),
    onConnectionStateChange([&]() -> CachedProp<std::optional<std::function<void(const ConnectionStateEvent& /* event */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onConnectionStateChange", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onConnectionStateChange;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const ConnectionStateEvent& /* event */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onConnectionStateChange);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("EllaTerminalView.onConnectionStateChange: ") + exc.what());
      }
    }()),
    onControlModifierChange([&]() -> CachedProp<std::optional<std::function<void(bool /* active */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onControlModifierChange", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onControlModifierChange;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(bool /* active */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onControlModifierChange);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("EllaTerminalView.onControlModifierChange: ") + exc.what());
      }
    }()),
    onHostKeyRequest([&]() -> CachedProp<std::optional<std::function<void(const HostKeyRequestEvent& /* event */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onHostKeyRequest", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onHostKeyRequest;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const HostKeyRequestEvent& /* event */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onHostKeyRequest);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("EllaTerminalView.onHostKeyRequest: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridEllaTerminalViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridEllaTerminalViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("EllaTerminalView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridEllaTerminalViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("headerInset"): return true;
      case hashString("onConnectionStateChange"): return true;
      case hashString("onControlModifierChange"): return true;
      case hashString("onHostKeyRequest"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridEllaTerminalViewComponentDescriptor::HybridEllaTerminalViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridEllaTerminalViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                            const std::shared_ptr<const react::Props>& props,
                                                                                            react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridEllaTerminalViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridEllaTerminalViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridEllaTerminalViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridEllaTerminalViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridEllaTerminalViewProps>& props = std::const_pointer_cast<HybridEllaTerminalViewProps>(constProps);
    HybridEllaTerminalViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::ella::terminal::views
