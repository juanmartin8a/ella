///
/// HybridEllaTerminalViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridEllaTerminalViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridEllaTerminalViewSpecSwift.hpp"
#import "EllaTerminal-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::ella::terminal;
using namespace margelo::nitro::ella::terminal::views;

/**
 * Represents the React Native View holder for the Nitro "EllaTerminalView" HybridView.
 */
@interface HybridEllaTerminalViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridEllaTerminalViewComponent {
  std::shared_ptr<HybridEllaTerminalViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridEllaTerminalViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridEllaTerminalViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridEllaTerminalViewSpec> hybridView = EllaTerminal::EllaTerminalAutolinking::createEllaTerminalView();
    _hybridView = std::dynamic_pointer_cast<HybridEllaTerminalViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  EllaTerminal::HybridEllaTerminalViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridEllaTerminalViewProps const>(props);
  auto& newViewProps = const_cast<HybridEllaTerminalViewProps&>(newViewPropsConst);
  EllaTerminal::HybridEllaTerminalViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // headerInset: number
  if (newViewProps.headerInset.isDirty) {
    swiftPart.setHeaderInset(newViewProps.headerInset.value);
    newViewProps.headerInset.isDirty = false;
  }
  // onConnectionStateChange: optional
  if (newViewProps.onConnectionStateChange.isDirty) {
    swiftPart.setOnConnectionStateChange(newViewProps.onConnectionStateChange.value);
    newViewProps.onConnectionStateChange.isDirty = false;
  }
  // onControlModifierChange: optional
  if (newViewProps.onControlModifierChange.isDirty) {
    swiftPart.setOnControlModifierChange(newViewProps.onControlModifierChange.value);
    newViewProps.onControlModifierChange.isDirty = false;
  }
  // onHostKeyRequest: optional
  if (newViewProps.onHostKeyRequest.isDirty) {
    swiftPart.setOnHostKeyRequest(newViewProps.onHostKeyRequest.value);
    newViewProps.onHostKeyRequest.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return EllaTerminal::EllaTerminalAutolinking::isEllaTerminalViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  EllaTerminal::HybridEllaTerminalViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  EllaTerminal::HybridEllaTerminalViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
