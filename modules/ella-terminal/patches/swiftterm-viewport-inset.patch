diff --git a/Sources/SwiftTerm/Apple/AppleTerminalView.swift b/Sources/SwiftTerm/Apple/AppleTerminalView.swift
index c2fdd81..9b6f1d7 100644
--- a/Sources/SwiftTerm/Apple/AppleTerminalView.swift
+++ b/Sources/SwiftTerm/Apple/AppleTerminalView.swift
@@ -145,9 +145,14 @@ extension TerminalView {
     {
         resetCaches()
         self.cellDimension = computeFontDimensions ()
         if (frame.width > 0) && (frame.height > 0) {
             let newCols = Int(frame.width / cellDimension.width)
-            let newRows = Int(frame.height / cellDimension.height)
+            #if os(iOS) || os(visionOS)
+            let viewportHeight = max(0, frame.height - viewportTopInset)
+            #else
+            let viewportHeight = frame.height
+            #endif
+            let newRows = max(1, Int(viewportHeight / cellDimension.height))
             resize(cols: newCols, rows: newRows)
         }
         updateCaretView()
@@ -178,11 +183,16 @@ extension TerminalView {
         // Get the ascent + descent + leading from the font, already scaled for the font's size
         self.cellDimension = computeFontDimensions ()
 
         let zeroSizedView = width == 0 && height == 0
+        #if os(iOS) || os(visionOS)
+        let viewportHeight = max(0, height - viewportTopInset)
+        #else
+        let viewportHeight = height
+        #endif
         let terminalOptions = zeroSizedView
             ? (terminal?.options ?? .default)
             : TerminalOptions(cols: Int(width / cellDimension.width),
-                              rows: Int(height / cellDimension.height))
+                              rows: max(1, Int(viewportHeight / cellDimension.height)))
 
         if terminal == nil {
             terminal = Terminal(delegate: self, options: terminalOptions)
@@ -225,7 +235,12 @@ extension TerminalView {
         if newSize.width == 0 && newSize.height == 0 {
             return false
         }
-        let newRows = Int (newSize.height / cellDimension.height)
+        #if os(iOS) || os(visionOS)
+        let viewportHeight = max(0, newSize.height - viewportTopInset)
+        #else
+        let viewportHeight = newSize.height
+        #endif
+        let newRows = max(1, Int(viewportHeight / cellDimension.height))
         let newCols = Int (getEffectiveWidth (size: newSize) / cellDimension.width)
 
         if newCols != terminal.cols || newRows != terminal.rows {
diff --git a/Sources/SwiftTerm/iOS/iOSTerminalView.swift b/Sources/SwiftTerm/iOS/iOSTerminalView.swift
index 98a040c..0352269 100644
--- a/Sources/SwiftTerm/iOS/iOSTerminalView.swift
+++ b/Sources/SwiftTerm/iOS/iOSTerminalView.swift
@@ -161,6 +161,7 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
     private var pointerInteraction: UIPointerInteraction?
     private var hoverGesture: UIHoverGestureRecognizer?
     private var didFinishSetup = false
+    private var _viewportTopInset: CGFloat = 0
     var linkHighlightRange: [Terminal.LinkMatch.RowRange]?
     private var lastPointerLocation: CGPoint?
     
@@ -243,6 +244,36 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
     var transparent = TTColor.transparent ()
     private var lastLayoutBounds: CGRect = .zero
     
+    /// The portion of the view obscured by an overlay at its top edge.
+    /// Content can scroll beneath the overlay while terminal rows and the scroll
+    /// indicator use only the unobscured viewport.
+    public var viewportTopInset: CGFloat {
+        get { _viewportTopInset }
+        set {
+            let inset = max(0, newValue)
+            guard inset != _viewportTopInset else { return }
+
+            _viewportTopInset = inset
+            contentInsetAdjustmentBehavior = .never
+            automaticallyAdjustsScrollIndicatorInsets = false
+            contentInset.top = inset
+            verticalScrollIndicatorInsets.top = inset
+            #if canImport(MetalKit)
+            if inset > 0, useMetalRenderer {
+                try? updateMetalRenderer(enabled: false)
+                useMetalRenderer = false
+            }
+            #endif
+
+            guard didFinishSetup else { return }
+            if !bounds.isEmpty {
+                _ = processSizeChange(newSize: bounds.size)
+            }
+            updateScroller()
+            requestDisplay()
+        }
+    }
+
     // UITextInput support starts
     public lazy var tokenizer: UITextInputTokenizer = UITextInputStringTokenizer (textInput: self) // TerminalInputTokenizer()
     
@@ -362,4 +393,7 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
     public func setUseMetal(_ enabled: Bool) throws {
+        if enabled && viewportTopInset > 0 {
+            throw MetalError.viewportTopInsetUnsupported
+        }
         if enabled == useMetalRenderer {
             return
         }
@@ -1483,10 +1517,11 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
             return
         }
         let rowOffset = CGFloat (displayBuffer.yDisp) * cellDimension.height
-        let desiredY = userScrolling ? rowOffset + manualScrollOffsetWithinRow : rowOffset
+        let logicalOffset = userScrolling ? rowOffset + manualScrollOffsetWithinRow : rowOffset
+        let desiredY = logicalOffset - viewportTopInset
         // Clamp to the scroll view's real maximum so following the bottom rests
         // flush against the last line instead of over-scrolling past it.
-        let offsetY = min(desiredY, maxContentOffsetY())
+        let offsetY = min(max(desiredY, minContentOffsetY()), maxContentOffsetY())
         setContentOffsetFromTerminal(CGPoint (x: 0, y: offsetY))
         //Xscroller.doubleValue = scrollPosition
         //Xscroller.knobProportion = scrollThumbsize
@@ -1519,6 +1554,10 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
         1 / max(backingScaleFactor(), 1)
     }
 
+    private func minContentOffsetY() -> CGFloat {
+        -viewportTopInset
+    }
+
     private func maxDisplayRow(in displayBuffer: Buffer) -> Int {
         max(0, displayBuffer.lines.count - displayBuffer.rows)
     }
@@ -1534,7 +1573,7 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
     /// shifts, and ignoring it left the user unable to ever reach the bottom to
     /// disengage the freeze — even by overscrolling.
     private func maxContentOffsetY() -> CGFloat {
-        max(0, contentSize.height - bounds.height + adjustedContentInset.bottom)
+        max(minContentOffsetY(), contentSize.height - bounds.height + adjustedContentInset.bottom)
     }
 
     private func setContentOffsetFromTerminal(_ newContentOffset: CGPoint) {
@@ -1565,6 +1604,7 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
         let displayBuffer = terminal.displayBuffer
         terminal.setViewYDisp(maxDisplayRow(in: displayBuffer))
-        let bottomOffset = min(CGFloat(displayBuffer.yDisp) * cellDimension.height, maxContentOffsetY())
+        let desiredOffset = CGFloat(displayBuffer.yDisp) * cellDimension.height - viewportTopInset
+        let bottomOffset = min(max(desiredOffset, minContentOffsetY()), maxContentOffsetY())
         setContentOffsetFromTerminal(CGPoint(x: 0, y: bottomOffset))
     }
 
@@ -1572,12 +1612,13 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
-    private func syncYDispFromContentOffset() {
+    private func syncYDispFromContentOffset(allowProgrammaticScroll: Bool = false) {
         guard terminal != nil, !updatingContentOffsetFromTerminal, cellDimension.height > 0 else {
             return
         }
 
         let displayBuffer = terminal.displayBuffer
         let maxRow = maxDisplayRow(in: displayBuffer)
         let maxContentOffset = maxContentOffsetY()
-        let offsetY = min(max(contentOffset.y, 0), maxContentOffset)
+        let offsetY = min(max(contentOffset.y, minContentOffsetY()), maxContentOffset)
+        let logicalOffsetY = max(0, offsetY + viewportTopInset)
 
         // A drag that lands within half a row of the bottom (or overscrolls past
         // it) re-engages auto-follow. A sub-pixel tolerance was too tight —
@@ -1595,14 +1636,7 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
-        // Freeze auto-follow only while the finger is physically down
-        // (isTracking). Excluding the momentum coast is essential: after the
-        // finger lifts, deceleration keeps firing sync while streaming output
-        // extends the content and the bottom recedes ahead of the coasting
-        // offset — treating that "not at the bottom yet" reading as a manual
-        // scroll would re-freeze a view the user just flung to the bottom. This
-        // must key off isTracking, not isDragging: on device isDragging stays
-        // true through the entire coast, so it fails to exclude momentum. It also
-        // covers layout/system-driven offset changes (startup sizing, rotation,
-        // keyboard insets, buffer shrink), which are never a manual scroll.
-        guard isTracking else {
+        // Only a finger-down gesture may start manual scrolling. Once active,
+        // keep the terminal model synchronized through momentum. Accessibility
+        // paging opts in explicitly; layout-driven changes never do.
+        guard isTracking || (userScrolling && isDecelerating) || allowProgrammaticScroll else {
             return
         }
 
@@ -1606,8 +1643,8 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
             return
         }
 
-        let row = max(0, min(maxRow, Int(floor((offsetY + contentOffsetTolerance) / cellDimension.height))))
-        manualScrollOffsetWithinRow = offsetY - CGFloat(row) * cellDimension.height
+        let row = max(0, min(maxRow, Int(floor((logicalOffsetY + contentOffsetTolerance) / cellDimension.height))))
+        manualScrollOffsetWithinRow = logicalOffsetY - CGFloat(row) * cellDimension.height
         if displayBuffer.yDisp != row {
             terminal.setViewYDisp(row)
         }
@@ -1705,15 +1746,15 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
     }
 
     open override func accessibilityScroll(_ direction: UIAccessibilityScrollDirection) -> Bool {
-        let pageHeight = max(bounds.height, cellDimension.height)
-        let maxOffsetY = max(0, contentSize.height - bounds.height)
+        let pageHeight = max(bounds.height - viewportTopInset, cellDimension.height)
+        let maxOffsetY = maxContentOffsetY()
         let targetOffsetY: CGFloat
 
         switch direction {
         case .down, .right, .next:
             targetOffsetY = min(maxOffsetY, contentOffset.y + pageHeight)
         case .up, .left, .previous:
-            targetOffsetY = max(0, contentOffset.y - pageHeight)
+            targetOffsetY = max(minContentOffsetY(), contentOffset.y - pageHeight)
         default:
             return super.accessibilityScroll(direction)
         }
@@ -1719,6 +1760,7 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
         }
 
         setContentOffset(CGPoint(x: contentOffset.x, y: targetOffsetY), animated: false)
+        syncYDispFromContentOffset(allowProgrammaticScroll: true)
         setNeedsDisplay(bounds)
         // Based on WWDC 2019 presentation: argument is nil
         UIAccessibility.post(notification: .pageScrolled, argument: nil)
@@ -3132,12 +3173,15 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
     }
 
     public func accessibilityPageContent() -> String? {
-        let pageHeight = max(bounds.height, cellDimension.height)
-        let lines = Int(floor(pageHeight/cellDimension.height))
-        let startLine = Int(floor(contentOffset.y / cellDimension.height))
+        let buffer = terminal.buffer
+        guard !buffer.lines.isEmpty else { return nil }
+        let pageHeight = max(bounds.height - viewportTopInset, cellDimension.height)
+        let lines = max(1, Int(floor(pageHeight/cellDimension.height)))
+        let logicalOffset = max(0, contentOffset.y + viewportTopInset)
+        let startLine = min(buffer.lines.count - 1, Int(floor(logicalOffset / cellDimension.height)))
+        let endLine = min(buffer.lines.count - 1, startLine + lines - 1)
         let start = Position(col: 0, row: startLine)
-        let end = Position(col: terminal.buffer.lines[startLine].count,
-                           row: startLine + lines)
+        let end = Position(col: buffer.lines[endLine].count, row: endLine)
         return terminal.getDisplayText(start: start, end: end)
     }
 
@@ -3151,12 +3195,15 @@ open class TerminalView: UIScrollView, UITextInputTraits, UIKeyInput, UIScrollVi
     }
 
     public func accessibilityAttributedPageContent() -> NSAttributedString? {
-        let pageHeight = max(bounds.height, cellDimension.height)
-        let lines = Int(floor(pageHeight/cellDimension.height))
-        let startLine = Int(floor(contentOffset.y / cellDimension.height))
+        let buffer = terminal.buffer
+        guard !buffer.lines.isEmpty else { return nil }
+        let pageHeight = max(bounds.height - viewportTopInset, cellDimension.height)
+        let lines = max(1, Int(floor(pageHeight/cellDimension.height)))
+        let logicalOffset = max(0, contentOffset.y + viewportTopInset)
+        let startLine = min(buffer.lines.count - 1, Int(floor(logicalOffset / cellDimension.height)))
+        let endLine = min(buffer.lines.count - 1, startLine + lines - 1)
         let start = Position(col: 0, row: startLine)
-        let end = Position(col: terminal.buffer.lines[startLine].count,
-                           row: startLine + lines)
+        let end = Position(col: buffer.lines[endLine].count, row: endLine)
         return accessibilityAttributedDisplayText(start: start, end: end)
     }
 }
diff --git a/Sources/SwiftTerm/Apple/Metal/MetalError.swift b/Sources/SwiftTerm/Apple/Metal/MetalError.swift
index d312d31..77f71ea 100644
--- a/Sources/SwiftTerm/Apple/Metal/MetalError.swift
+++ b/Sources/SwiftTerm/Apple/Metal/MetalError.swift
@@ -18,6 +18,7 @@ public enum MetalError: Error, CustomStringConvertible {
     case shaderCompilationFailed(String)
     case pipelineCreationFailed(String)
     case samplerUnavailable
+    case viewportTopInsetUnsupported
 
     public var description: String {
         switch self {
@@ -43,6 +44,8 @@ public enum MetalError: Error, CustomStringConvertible {
             return "Failed to create Metal pipeline: \(name)"
         case .samplerUnavailable:
             return "Failed to create Metal sampler state."
+        case .viewportTopInsetUnsupported:
+            return "Metal rendering does not support a top viewport inset."
         }
     }
 }
